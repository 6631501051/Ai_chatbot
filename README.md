# Cinephile Movie Radar

A RAG-powered movie recommendation engine using OpenThaiGPT and LangChain.

## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Ensure you have a GPU (required for 4-bit quantization)
4. Run the app: `python app.py`
